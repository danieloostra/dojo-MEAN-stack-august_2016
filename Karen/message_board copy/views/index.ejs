<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Message Board</title>
        <style>
        *{
            text-align: center;
        }
        </style>
    </head>
    <body>
        <h1>The Dojo Message Board</h1><hr><br>

        <% if(typeof(errors) != 'undefined' ){ %>
            <% for (var x in errors) { %>
                <h3> <%= errors[x].message %> </h3>
            <% } %>
        <% } %>

        <form action ="/messages/add" method = "post">
            <input type="text" name= "name" placeholder="YOUR NAME"><br><br>
            <textarea rows = "4" cols= "20" name = "message" placeholder="YOUR MESSAGE"></textarea><br>
            <input type="submit" value ="Post message."> <br><br>
        </form>

        <hr>

        <% for(var i = 0; i < info.length; i++){ %>
            <h2>NAME: <%=info[i].name%></h2>
            <h2>MESSAGE: <%=info[i].message%> </h2>
            <%for(var j= 0; j < info[i]._comments.length; j++){%>
            <p> <%= info[i]._comments[j].name%> :                                     <%=info[i]._comments[j].comment %></p>
            <%}%>

            <form action = "/messages/addcomment/<%=info[i]._id%>" method= "post">
                <input type="text" name= "name" placeholder="YOUR NAME"><br><br>
                <textarea rows = "4" cols= "20" name = "comment" placeholder="YOUR COMMENT"></textarea><br>
                <input type="submit" value ="Post Comment.">
            </form>
        <% } %>

    </body>
</html>
